using UnityEngine;

namespace stealFight {

    public class MainSceneInit : SceneBase {
        // Use this for initialization
        new void Start() {
            base.Start();
        }

        // Update is called once per frame
        new void Update() {
        }
    }

}